<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>User index</title>
</head>
<body>
    <h1>Handlebars</h1>
    <ul>
        {{#each users as |user|}}
            <li><a href="/{{user.username}}">{{user.name.full}}</a>
        {{/each}}
    </ul>
</body>
</html>